import { File } from '../ddo/MetaData';
import Account from '../ocean/Account';
import { Instantiable, InstantiableConfig } from '../Instantiable.abstract';
export declare class Brizo extends Instantiable {
    private get url();
    constructor(config: InstantiableConfig);
    getVersionInfo(): Promise<any>;
    getPurchaseEndpoint(): string;
    getConsumeEndpoint(): string;
    getEncryptEndpoint(): string;
    getComputeEndpoint(pubKey: string, serviceIndex: number, _notUsed: string, container: string): string;
    initializeServiceAgreement(did: string, serviceAgreementId: string, serviceIndex: number, signature: string, consumerAddress: string): Promise<any>;
    consumeService(agreementId: string, serviceEndpoint: string, account: Account, files: File[], destination: string, index?: number): Promise<string>;
    encrypt(did: string, signature: string, document: any, publisher: string): Promise<string>;
}
