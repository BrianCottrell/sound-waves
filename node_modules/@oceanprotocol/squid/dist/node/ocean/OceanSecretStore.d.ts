import Account from './Account';
import { Instantiable, InstantiableConfig } from '../Instantiable.abstract';
export declare class OceanSecretStore extends Instantiable {
    static getInstance(config: InstantiableConfig): Promise<OceanSecretStore>;
    encrypt(did: string, document: any, publisher: Account): Promise<string>;
    decrypt(did: string, content: string, consumer?: Account, secretStoreUrl?: string): Promise<any>;
    private getSecretStoreByAccount;
    private getSecretStore;
}
